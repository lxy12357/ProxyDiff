# ProxyDiff

ProxyDiff is a proxy-guided differentiable scoring method for structural
component selection.

## Reproduction Code

The current public reproduction package contains the NB301 NAS pipeline:

```text
reproduction/nas/
```

See `reproduction/nas/README.md` for the end-to-end score, cache, refinement,
and evaluation commands.
